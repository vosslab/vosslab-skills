interface:
  display_name: "Parallel Plan"
  short_description: "Lightweight split-and-dispatch planning"
  default_prompt: "Use $parallel-plan as a lightweight manager profile: split this active task into independent concurrent workstreams with owners, dependencies, work packages, patch plan, and checkpoints."
