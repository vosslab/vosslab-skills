interface:
  display_name: "Repo Rules Reader"
  short_description: "Load repository rules into context"
  default_prompt: "Use $repo-rules-reader to read AGENTS.md, docs/*_STYLE.md, docs/CLAUDE_HOOK_USAGE_GUIDE.md, and the latest docs/CHANGELOG.md entry. Give the concise read receipt, then continue the task or be ready for a task to be provided."
