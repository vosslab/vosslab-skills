interface:
  display_name: "Blueprint Plan Drafter"
  short_description: "Draft forward-looking implementation plans"
  default_prompt: "Use $blueprint-plan-drafter to create a new implementation plan without writing code, following repository rules and using the sections appropriate to the work."
