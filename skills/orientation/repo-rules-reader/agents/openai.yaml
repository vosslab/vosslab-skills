interface:
  display_name: "Repo Rules Reader"
  short_description: "Load repository rules into context"
  default_prompt: "Use $repo-rules-reader to load the required repository rules and latest changelog entry, give the concise read receipt, then continue the task."
