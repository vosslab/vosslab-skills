{
	"name": "web-game-parallel-builder-project",
	"version": "0.0.1",
	"private": true,
	"description": "Scaffold for a web-game-parallel-builder TypeScript project.",
	"scripts": {
		"setup": "./setup_game.sh",
		"setup:playwright": "./setup_playwright.sh",
		"build": "./build_github_pages.sh",
		"export:single": "./export_single_file.sh",
		"serve": "./run_web_server.sh"
	},
	"devDependencies": {
		"esbuild": "^0.24.0",
		"typescript": "^5.6.0"
	}
}
